# Invalid run

This run is excluded from pipeline evidence.

- The installed Codex CLI was incompatible with the desktop-written model cache and exited during finalization.
- A research card was left behind, but the runner did not return a trustworthy token total.
- No downstream author stage may use this research card.

